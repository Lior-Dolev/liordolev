// import { RouteComponentProps } from 'react-router-dom';

export type PageProps = {
  className?: string;
  id?: string;
  tabIndex?: number;
};

// { history: History<{}>; location: Location<{}>; match: match<any>; staticContext?: StaticContext; }
